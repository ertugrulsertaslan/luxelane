<div class="container">
    <div class="row">
        <div class="col text-center">
            <div style="color:green">
                <h1>Successful</h1>
                <h5>{{message}}</h5>
            </div>
        </div>
    </div>
</div>

<script>
    const REDIRECT_DELAY = {{delay}} || 5000;
    setTimeout(() => {
        window.location.replace('{{redirect}}');
    }, REDIRECT_DELAY);
</script>